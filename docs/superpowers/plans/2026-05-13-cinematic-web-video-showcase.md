# Cinematic Web Video Showcase Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an independent `cinematic-ai-launch` showcase that makes `web-video-presentation` feel like a real product launch video with strong motion, transitions, and lightweight sound cues.

**Architecture:** Keep the existing scaffolded Vite + React + TypeScript presentation architecture. Add one standalone showcase directory with content artifacts, a scaffolded `presentation/`, one custom eight-step chapter, local CSS/SVG motion, and showcase-local sound cue playback.

**Tech Stack:** Vite, React, TypeScript, CSS keyframes, SVG, Web Audio API-generated WAV assets, existing `web-video-presentation` scaffold.

---

## File Structure

Create this new directory tree:

```text
showcases/cinematic-ai-launch/
├── article.md
├── script.md
├── outline.md
├── motion-treatment.md
├── sound-plan.md
└── presentation/
    ├── package.json
    ├── public/
    │   └── sfx/
    │       ├── data-pop.wav
    │       ├── lock.wav
    │       ├── low-hit.wav
    │       ├── room-tone.wav
    │       ├── soft-whoosh.wav
    │       └── tick.wav
    ├── scripts/
    │   └── make-sfx.mjs
    └── src/
        ├── chapters/
        │   └── 01-cinematic-ai-launch/
        │       ├── CinematicAiLaunch.css
        │       ├── CinematicAiLaunch.tsx
        │       └── narrations.ts
        └── registry/
            └── chapters.ts
```

Responsibilities:

- `article.md`: commercial source article used as the raw content.
- `script.md`: spoken 60-90 second launch narration, split by `---`.
- `outline.md`: eight-step screen-content plan, no implementation-level animation details.
- `motion-treatment.md`: per-step action design and anti-PPT risks.
- `sound-plan.md`: sound cue intent and cue-to-step mapping.
- `presentation/`: standalone scaffolded app generated by `scripts/scaffold.sh`.
- `make-sfx.mjs`: deterministic local WAV generation so the showcase has sound without external binary assets.
- `CinematicAiLaunch.tsx`: one isolated chapter component with eight visual steps and local step SFX playback.
- `CinematicAiLaunch.css`: chapter-only styling with `.cal-` prefix, semantic theme tokens, SVG/CSS motion.
- `narrations.ts`: single source of truth for the eight step count and narration text.
- `chapters.ts`: registers only the showcase chapter.

---

### Task 1: Create Showcase Content Artifacts

**Files:**
- Create: `showcases/cinematic-ai-launch/article.md`
- Create: `showcases/cinematic-ai-launch/script.md`
- Create: `showcases/cinematic-ai-launch/outline.md`
- Create: `showcases/cinematic-ai-launch/motion-treatment.md`
- Create: `showcases/cinematic-ai-launch/sound-plan.md`

- [ ] **Step 1: Create the showcase directory**

Run:

```bash
mkdir -p showcases/cinematic-ai-launch
```

Expected: directory exists at `showcases/cinematic-ai-launch`.

- [ ] **Step 2: Write `article.md`**

Create `showcases/cinematic-ai-launch/article.md`:

```markdown
# Article In. Video Out.

Most teams already have the raw material for a good video. It is sitting in launch notes, product updates, technical blogs, internal memos, and long-form articles.

The hard part is not writing the first draft. The hard part is turning that draft into something people will actually watch.

The old workflow breaks the job into too many pieces. You rewrite the article into a script. You make slides. You hunt for images. You record voiceover. You open a video editor. Then you try to make the final result feel less like a slide deck.

That is where most content loses its shape. The story was clear in the article, but the video becomes a stack of titles, bullet points, and transitions.

`web-video-presentation` treats the article as the beginning of a video pipeline. The article becomes a spoken script. The script becomes beats. Each beat becomes a full-screen visual step. Motion is designed around the idea being explained, not around a generic entrance animation.

The result is still a web page, but it behaves like a recordable video stage. It has a fixed 16:9 canvas, hidden playback controls, audio-aware steps, and a rhythm that can be captured in one take.

This showcase demonstrates the target: a short product-launch style sequence where the article enters the stage, becomes narration, becomes motion, locks to sound cues, and exits as a finished video.
```

- [ ] **Step 3: Write `script.md`**

Create `showcases/cinematic-ai-launch/script.md`:

```markdown
# Script

一篇好文章，不应该只停在页面里。

它本来就有观点，有节奏，也有故事。

缺的不是内容，缺的是把它变成视频的那条路。

---

以前你想做这件事，要拆成一堆工具。

先改口播，再做 PPT，再找素材，再配音，最后还要剪辑。

忙了一圈，成片还是像一组会动的幻灯片。

---

所以我们换一种做法。

让文章直接进入一个视频舞台。

它先变成脚本，再变成 outline，再进入 motion 和 audio。

---

脚本不是普通文本。

每一段口播，都会变成一个节拍。

每个节拍，对应屏幕上的一次推进。

---

真正的视频感，不是让文字飞进来。

而是每一步都在演一件事。

数据流动，路径画出来，流程点亮，界面状态发生变化。

---

然后声音进来。

一个 hit，一个 tick，一个 whoosh。

点击不再像翻页，而像剪辑点。

---

等节拍、画面、声音都锁住以后。

打开自动模式，录屏一镜到底。

网页就变成了一条能发布的视频。

---

Article in. Video out.

这不是 PPT。

这是一个可录屏的视频舞台。
```

- [ ] **Step 4: Write `outline.md`**

Create `showcases/cinematic-ai-launch/outline.md`:

```markdown
# Video Outline

> **主题**：`midnight-press` — 暗色电影感技术发布。
> **总时长**：约 75 秒（口播约 300 字 ÷ 4 字/秒）。
> **章节数**：1 章 / 8 步。

---

## 1. cinematic-ai-launch — Article In. Video Out.（8 steps · ~75s）

**信息池**：
- 问题：文章、产品更新、技术博客常常已经有视频素材的内容基础 —— 来源 article §1
- 旧流程：改口播、做 PPT、找素材、配音、剪辑五段断裂流程 —— 来源 article §3
- 损耗：清晰故事容易变成标题、项目符号和转场堆叠 —— 来源 article §4
- 新流程：article → script → beats → visual steps → recordable stage —— 来源 article §5-6
- 差异：动效围绕正在解释的想法设计，而不是套通用入场动画 —— 来源 article §5

**开发计划**：

- step 1 (~10s) — 黑场中一篇长文落入舞台，文章碎片悬浮，打出 `Article In. Video Out.`
- step 2 (~10s) — 旧工作流裂成五个孤岛：口播、PPT、素材、配音、剪辑。
- step 3 (~9s) — 文章进入中央 pipeline，script / outline / motion / audio 节点依次点亮。
- step 4 (~9s) — 口播文本被切成 beat blocks，每个 block 对应一个 step。
- step 5 (~12s) — beat blocks 变成不同 motion treatments：数据流、路径、对比、终端、UI。
- step 6 (~8s) — 声音 cue 进入时间线：hit / tick / whoosh / lock 和 narration 对齐。
- step 7 (~9s) — 自动播放轨道推进，录屏框锁住 16:9 舞台。
- step 8 (~8s) — article.md → script.md → motion-treatment.md → presentation → video.mp4 链路定格。

口播节选：
> 一篇好文章，不应该只停在页面里。
> 真正的视频感，不是让文字飞进来。而是每一步都在演一件事。
> 这不是 PPT。这是一个可录屏的视频舞台。

---

## 素材清单

### 1. cinematic-ai-launch
- ✓ 视觉素材全部用 CSS / SVG 生成，不依赖外部图片。
- ✓ 音效用 `scripts/make-sfx.mjs` 生成本地 WAV 文件。
- ✓ 背景、字体、颜色从 `midnight-press` token 继承。
```

- [ ] **Step 5: Write `motion-treatment.md`**

Create `showcases/cinematic-ai-launch/motion-treatment.md`:

```markdown
# Motion Treatment

## Intent

This chapter must feel like a product launch short, not a slide deck. Every step performs a visible transformation. Text is used as labeling and emphasis, not as the only thing happening on screen.

## Step Treatment

| Step | Narrative Beat | Dominant Action | Transition In | Transition Out | Persistent Object | Extra Visual Information | Anti-PPT Risk |
|---|---|---|---|---|---|---|---|
| 1 | A good article should become more than a page. | Article file impacts the stage; text fragments scatter. | Black field wakes with a low glow. | Fragments pull toward workflow islands. | Article file / text fragments. | `Article In. Video Out.` appears as product lockup. | Static title card. |
| 2 | The old workflow is fragmented. | Five islands split apart and lines tangle. | Text fragments become island labels. | Tangled lines collapse into one pipeline spine. | Workflow labels. | Islands: script, PPT, assets, voice, edit. | Five static cards. |
| 3 | The article enters a video-stage pipeline. | Pipeline nodes ignite in sequence. | Spine line straightens and lights up. | Nodes compress into beat blocks. | Pipeline spine. | Nodes: script, outline, motion, audio, recording. | Simple process diagram. |
| 4 | Script becomes beats. | Paragraphs slice into beat blocks. | Pipeline output becomes text strips. | Blocks fan into motion cards. | Beat blocks. | Each block has a step number and pulse. | Bullet list of steps. |
| 5 | Beats become visual actions. | Motion cards morph into demos. | Beat blocks flip into cards. | Cards snap onto an audio timeline. | Motion cards. | Data flow, path trace, split compare, terminal replay, UI state. | Feature grid. |
| 6 | Sound turns clicks into edit points. | Audio lane forms; cue markers snap to beats. | Motion cards become cue points. | Timeline expands into recording track. | Cue timeline. | hit, tick, whoosh, lock, ambience. | Static waveform decoration. |
| 7 | Auto mode records the stage in one take. | Cursor advances across a recording rail; frame locks around stage. | Timeline grows to full width. | Output frame collapses into artifact chain. | Recording frame. | `?auto=1`, hidden chrome, one-take recording. | Generic progress bar. |
| 8 | The pipeline becomes a finished video. | Artifacts converge into final lockup. | Recording frame becomes output file. | End state holds. | Artifact chain. | `article.md -> script.md -> motion-treatment.md -> presentation -> video.mp4`. | Static closing slide. |

## Motion Rules

- Every scene needs a moving object that explains the beat.
- At least one visual object must carry continuity between adjacent steps.
- Use CSS/SVG first. Use Canvas only if the text-fragment scatter looks too mechanical.
- Keep the stage dark, spacious, and editorial. Do not add colorful UI cards.
- Motion should land before the viewer needs to read the final text.
```

- [ ] **Step 6: Write `sound-plan.md`**

Create `showcases/cinematic-ai-launch/sound-plan.md`:

```markdown
# Sound Plan

## Intent

Sound cues should make a click feel like an edit point. They must stay subtle and should not compete with narration. The first version uses generated WAV files and local playback on step entry.

## Cue Map

| Step | Cue ID | File | Level | Purpose |
|---|---|---|---|---|
| 1 | low-hit | `public/sfx/low-hit.wav` | medium | Article lands on the stage. |
| 2 | soft-whoosh | `public/sfx/soft-whoosh.wav` | subtle | Workflow splits into islands. |
| 3 | data-pop | `public/sfx/data-pop.wav` | subtle | Pipeline nodes ignite. |
| 4 | tick | `public/sfx/tick.wav` | subtle | Script slices into beats. |
| 5 | data-pop | `public/sfx/data-pop.wav` | subtle | Motion cards activate. |
| 6 | lock | `public/sfx/lock.wav` | medium | Sound cues snap to the frame. |
| 7 | soft-whoosh | `public/sfx/soft-whoosh.wav` | subtle | Recording timeline opens. |
| 8 | lock | `public/sfx/lock.wav` | medium | Final output locks. |

## Implementation Notes

- Use `HTMLAudioElement` on step change.
- Start volume at `0.24`.
- Catch playback errors and continue navigation.
- Do not alter the base `useAudioPlayer` hook in the first version.
- Do not add background music in the first version.
```

- [ ] **Step 7: Commit content artifacts**

Run:

```bash
git add showcases/cinematic-ai-launch/article.md \
  showcases/cinematic-ai-launch/script.md \
  showcases/cinematic-ai-launch/outline.md \
  showcases/cinematic-ai-launch/motion-treatment.md \
  showcases/cinematic-ai-launch/sound-plan.md
git commit -m "Add cinematic AI launch showcase content"
```

Expected: commit succeeds with five new markdown files.

---

### Task 2: Scaffold The Standalone Presentation

**Files:**
- Create directory: `showcases/cinematic-ai-launch/presentation/`
- Generated by: `skills/web-video-presentation/scripts/scaffold.sh`

- [ ] **Step 1: Run the scaffold**

Run from repo root:

```bash
bash skills/web-video-presentation/scripts/scaffold.sh \
  showcases/cinematic-ai-launch/presentation \
  --theme=midnight-press
```

Expected:

```text
✓ 完成。下一步：
```

and the command runs `npx tsc --noEmit` successfully.

- [ ] **Step 2: Confirm generated files exist**

Run:

```bash
test -f showcases/cinematic-ai-launch/presentation/package.json
test -f showcases/cinematic-ai-launch/presentation/src/registry/chapters.ts
test -f showcases/cinematic-ai-launch/presentation/src/styles/tokens.css
test -f showcases/cinematic-ai-launch/presentation/src/chapters/01-example/Example.tsx
```

Expected: all commands exit with status `0`.

- [ ] **Step 3: Commit scaffold output**

Run:

```bash
git add showcases/cinematic-ai-launch/presentation
git commit -m "Scaffold cinematic AI launch presentation"
```

Expected: commit succeeds with scaffolded Vite presentation files.

---

### Task 3: Replace Example Chapter Registration

**Files:**
- Delete: `showcases/cinematic-ai-launch/presentation/src/chapters/01-example/Example.tsx`
- Delete: `showcases/cinematic-ai-launch/presentation/src/chapters/01-example/Example.css`
- Delete: `showcases/cinematic-ai-launch/presentation/src/chapters/01-example/narrations.ts`
- Create: `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/narrations.ts`
- Modify: `showcases/cinematic-ai-launch/presentation/src/registry/chapters.ts`

- [ ] **Step 1: Remove the scaffold example chapter**

Run:

```bash
rm -rf showcases/cinematic-ai-launch/presentation/src/chapters/01-example
mkdir -p showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch
```

Expected: `01-example` is gone and `01-cinematic-ai-launch` exists.

- [ ] **Step 2: Create `narrations.ts`**

Create `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/narrations.ts`:

```ts
export const narrations = [
  "一篇好文章，不应该只停在页面里。它本来就有观点，有节奏，也有故事。缺的不是内容，缺的是把它变成视频的那条路。",
  "以前你想做这件事，要拆成一堆工具。先改口播，再做 PPT，再找素材，再配音，最后还要剪辑。忙了一圈，成片还是像一组会动的幻灯片。",
  "所以我们换一种做法。让文章直接进入一个视频舞台。它先变成脚本，再变成 outline，再进入 motion 和 audio。",
  "脚本不是普通文本。每一段口播，都会变成一个节拍。每个节拍，对应屏幕上的一次推进。",
  "真正的视频感，不是让文字飞进来。而是每一步都在演一件事。数据流动，路径画出来，流程点亮，界面状态发生变化。",
  "然后声音进来。一个 hit，一个 tick，一个 whoosh。点击不再像翻页，而像剪辑点。",
  "等节拍、画面、声音都锁住以后。打开自动模式，录屏一镜到底。网页就变成了一条能发布的视频。",
  "Article in. Video out. 这不是 PPT。这是一个可录屏的视频舞台。",
] as const;
```

- [ ] **Step 3: Create a temporary placeholder component**

Create `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.tsx`:

```tsx
import type { ChapterStepProps } from "../../registry/types";
import "./CinematicAiLaunch.css";

export default function CinematicAiLaunch({ step }: ChapterStepProps) {
  return (
    <div className="cal-scene scene-pad">
      <div className="cal-kicker">Cinematic AI Launch</div>
      <h1 className="cal-temp-title">Step {step + 1}</h1>
      <p className="cal-temp-copy">
        Temporary chapter shell. The full cinematic scene is implemented in a later task.
      </p>
    </div>
  );
}
```

- [ ] **Step 4: Create temporary CSS**

Create `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.css`:

```css
.cal-scene {
  color: var(--text);
}

.cal-kicker {
  font-family: var(--font-mono);
  color: var(--accent);
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

.cal-temp-title {
  margin: 220px 0 24px;
  font-family: var(--font-display-en);
  font-size: 128px;
  line-height: 1;
}

.cal-temp-copy {
  max-width: 680px;
  color: var(--text-2);
  font-family: var(--font-body);
  font-size: 32px;
  line-height: 1.4;
}
```

- [ ] **Step 5: Replace `chapters.ts`**

Replace `showcases/cinematic-ai-launch/presentation/src/registry/chapters.ts`:

```ts
import type { ChapterDef } from "./types";
import CinematicAiLaunch from "../chapters/01-cinematic-ai-launch/CinematicAiLaunch";
import { narrations } from "../chapters/01-cinematic-ai-launch/narrations";

export const CHAPTERS: ChapterDef[] = [
  {
    id: "cinematic-ai-launch",
    title: "Article In. Video Out.",
    narrations,
    Component: CinematicAiLaunch,
  },
];
```

- [ ] **Step 6: Run typecheck**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
npx tsc --noEmit
```

Expected: no TypeScript errors.

- [ ] **Step 7: Commit chapter registration**

Run:

```bash
git add showcases/cinematic-ai-launch/presentation/src
git commit -m "Register cinematic AI launch chapter"
```

Expected: commit succeeds with example chapter removed and new chapter registered.

---

### Task 4: Add Showcase-Local Sound Assets

**Files:**
- Create: `showcases/cinematic-ai-launch/presentation/scripts/make-sfx.mjs`
- Create generated: `showcases/cinematic-ai-launch/presentation/public/sfx/*.wav`

- [ ] **Step 1: Create the SFX generation script**

Create `showcases/cinematic-ai-launch/presentation/scripts/make-sfx.mjs`:

```js
import { mkdirSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const sampleRate = 44100;
const outDir = new URL("../public/sfx/", import.meta.url);

function clamp(n) {
  return Math.max(-1, Math.min(1, n));
}

function env(t, duration, attack = 0.015, release = 0.16) {
  if (t < attack) return t / attack;
  if (t > duration - release) return Math.max(0, (duration - t) / release);
  return 1;
}

function writeWav(name, duration, render) {
  const frames = Math.floor(sampleRate * duration);
  const data = Buffer.alloc(frames * 2);

  for (let i = 0; i < frames; i += 1) {
    const t = i / sampleRate;
    const s = clamp(render(t, duration));
    data.writeInt16LE(Math.round(s * 32767), i * 2);
  }

  const header = Buffer.alloc(44);
  header.write("RIFF", 0);
  header.writeUInt32LE(36 + data.length, 4);
  header.write("WAVE", 8);
  header.write("fmt ", 12);
  header.writeUInt32LE(16, 16);
  header.writeUInt16LE(1, 20);
  header.writeUInt16LE(1, 22);
  header.writeUInt32LE(sampleRate, 24);
  header.writeUInt32LE(sampleRate * 2, 28);
  header.writeUInt16LE(2, 32);
  header.writeUInt16LE(16, 34);
  header.write("data", 36);
  header.writeUInt32LE(data.length, 40);

  mkdirSync(outDir, { recursive: true });
  writeFileSync(join(outDir.pathname, name), Buffer.concat([header, data]));
}

writeWav("low-hit.wav", 0.7, (t, d) => {
  const drop = Math.sin(2 * Math.PI * (78 - t * 42) * t);
  const click = Math.sin(2 * Math.PI * 190 * t) * Math.exp(-t * 34);
  return (drop * 0.52 + click * 0.25) * env(t, d, 0.004, 0.48);
});

writeWav("soft-whoosh.wav", 0.62, (t, d) => {
  const sweep = Math.sin(2 * Math.PI * (280 + t * 900) * t);
  const noise = Math.sin(2 * Math.PI * 41 * t) * Math.sin(2 * Math.PI * 113 * t);
  return (sweep * 0.16 + noise * 0.2) * env(t, d, 0.08, 0.22);
});

writeWav("tick.wav", 0.16, (t, d) => {
  const body = Math.sin(2 * Math.PI * 1300 * t) * Math.exp(-t * 55);
  return body * env(t, d, 0.002, 0.08) * 0.32;
});

writeWav("data-pop.wav", 0.24, (t, d) => {
  const a = Math.sin(2 * Math.PI * 620 * t);
  const b = Math.sin(2 * Math.PI * 930 * t) * Math.exp(-t * 22);
  return (a * 0.18 + b * 0.2) * env(t, d, 0.004, 0.12);
});

writeWav("lock.wav", 0.36, (t, d) => {
  const tone = Math.sin(2 * Math.PI * 220 * t) + Math.sin(2 * Math.PI * 440 * t) * 0.5;
  const snap = Math.sin(2 * Math.PI * 1200 * t) * Math.exp(-t * 42);
  return (tone * 0.22 + snap * 0.18) * env(t, d, 0.006, 0.2);
});

writeWav("room-tone.wav", 1.6, (t, d) => {
  const low = Math.sin(2 * Math.PI * 38 * t) * 0.16;
  const shimmer = Math.sin(2 * Math.PI * 73 * t) * Math.sin(2 * Math.PI * 121 * t) * 0.04;
  return (low + shimmer) * env(t, d, 0.25, 0.55);
});

console.log("Generated showcase SFX in public/sfx");
```

- [ ] **Step 2: Generate the WAV files**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
node scripts/make-sfx.mjs
```

Expected:

```text
Generated showcase SFX in public/sfx
```

- [ ] **Step 3: Confirm generated files**

Run:

```bash
ls public/sfx/*.wav
```

Expected output includes:

```text
public/sfx/data-pop.wav
public/sfx/lock.wav
public/sfx/low-hit.wav
public/sfx/room-tone.wav
public/sfx/soft-whoosh.wav
public/sfx/tick.wav
```

- [ ] **Step 4: Commit SFX assets and generator**

Run:

```bash
git add showcases/cinematic-ai-launch/presentation/scripts/make-sfx.mjs \
  showcases/cinematic-ai-launch/presentation/public/sfx
git commit -m "Add generated sound cues for showcase"
```

Expected: commit succeeds with one script and six WAV files.

---

### Task 5: Implement The Cinematic Chapter Component

**Files:**
- Modify: `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.tsx`

- [ ] **Step 1: Replace the placeholder component**

Replace `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.tsx`:

```tsx
import { useEffect, useMemo } from "react";
import type { ChapterStepProps } from "../../registry/types";
import "./CinematicAiLaunch.css";

const cues = [
  "low-hit",
  "soft-whoosh",
  "data-pop",
  "tick",
  "data-pop",
  "lock",
  "soft-whoosh",
  "lock",
] as const;

const pipeline = ["script", "outline", "motion", "audio", "record"];
const workflow = ["script", "PPT", "assets", "voice", "edit"];
const motionCards = [
  ["data flow", "signals move"],
  ["path trace", "logic draws"],
  ["split compare", "before / after"],
  ["terminal replay", "process runs"],
  ["UI state", "screen changes"],
];

function playCue(id: (typeof cues)[number]) {
  if (typeof window === "undefined") return;
  const audio = new Audio(`${import.meta.env.BASE_URL}sfx/${id}.wav`);
  audio.volume = id === "low-hit" || id === "lock" ? 0.28 : 0.2;
  audio.play().catch(() => {});
}

function FrameLabel({ step, label }: { step: number; label: string }) {
  return (
    <div className="cal-frame-label label-mono">
      <span>0{step + 1}</span>
      <span>{label}</span>
    </div>
  );
}

function TextFragments() {
  const fragments = [
    "launch notes",
    "product update",
    "technical blog",
    "article.md",
    "release memo",
    "case study",
    "long-form idea",
    "raw material",
  ];

  return (
    <div className="cal-fragments" aria-hidden>
      {fragments.map((fragment, index) => (
        <span key={fragment} style={{ "--i": index } as React.CSSProperties}>
          {fragment}
        </span>
      ))}
    </div>
  );
}

function StepOne() {
  return (
    <section className="cal-scene cal-step-one">
      <FrameLabel step={0} label="cold open" />
      <div className="cal-impact-ring" aria-hidden />
      <div className="cal-article-file">
        <div className="cal-file-top label-mono">article.md</div>
        <div className="cal-file-lines">
          <i />
          <i />
          <i />
          <i />
          <i />
        </div>
      </div>
      <TextFragments />
      <div className="cal-hero-lockup">
        <p className="cal-kicker">web video presentation</p>
        <h1>
          <span>Article In.</span>
          <span>Video Out.</span>
        </h1>
        <p>把文章变成一条能发布的视频</p>
      </div>
    </section>
  );
}

function StepTwo() {
  return (
    <section className="cal-scene cal-step-two">
      <FrameLabel step={1} label="old workflow" />
      <svg className="cal-tangle" viewBox="0 0 1920 1080" aria-hidden>
        <path d="M360 260 C720 120 740 620 1070 490 S1380 180 1560 360" />
        <path d="M420 800 C710 580 690 280 1010 360 S1330 810 1580 700" />
        <path d="M290 540 C610 530 760 880 1110 720 S1390 430 1650 520" />
      </svg>
      <div className="cal-islands">
        {workflow.map((item, index) => (
          <div className="cal-island card" key={item} style={{ "--i": index } as React.CSSProperties}>
            <span className="label-mono">0{index + 1}</span>
            <strong>{item}</strong>
          </div>
        ))}
      </div>
      <div className="cal-side-copy">
        <p className="cal-kicker">the old way</p>
        <h2>Too many tools. Too little rhythm.</h2>
      </div>
    </section>
  );
}

function StepThree() {
  return (
    <section className="cal-scene cal-step-three">
      <FrameLabel step={2} label="pipeline ignition" />
      <div className="cal-pipeline">
        <div className="cal-pipeline-source card">
          <span className="label-mono">input</span>
          <strong>article.md</strong>
        </div>
        <svg className="cal-pipe-line" viewBox="0 0 1120 180" aria-hidden>
          <path d="M20 90 H1100" />
        </svg>
        <div className="cal-pipeline-nodes">
          {pipeline.map((node, index) => (
            <div className="cal-node" key={node} style={{ "--i": index } as React.CSSProperties}>
              <span />
              <strong>{node}</strong>
            </div>
          ))}
        </div>
      </div>
      <h2 className="cal-statement">One stage. One rhythm. One path out.</h2>
    </section>
  );
}

function StepFour() {
  const beats = ["hook", "friction", "pipeline", "beats", "motion", "sound", "record", "lock"];
  return (
    <section className="cal-scene cal-step-four">
      <FrameLabel step={3} label="script to beats" />
      <div className="cal-script-panel card">
        <span className="label-mono">script.md</span>
        <p>一篇好文章，不应该只停在页面里。</p>
        <p>以前你想做这件事，要拆成一堆工具。</p>
        <p>真正的视频感，是每一步都在演一件事。</p>
      </div>
      <div className="cal-beat-stack">
        {beats.map((beat, index) => (
          <div className="cal-beat" key={beat} style={{ "--i": index } as React.CSSProperties}>
            <span className="label-mono">step {index + 1}</span>
            <strong>{beat}</strong>
          </div>
        ))}
      </div>
    </section>
  );
}

function StepFive() {
  return (
    <section className="cal-scene cal-step-five">
      <FrameLabel step={4} label="beats become motion" />
      <div className="cal-motion-grid">
        {motionCards.map(([title, caption], index) => (
          <div className="cal-motion-card card" key={title} style={{ "--i": index } as React.CSSProperties}>
            <span className="label-mono">{caption}</span>
            <strong>{title}</strong>
            <div className={`cal-mini cal-mini-${index}`} aria-hidden>
              <i />
              <i />
              <i />
            </div>
          </div>
        ))}
      </div>
      <h2 className="cal-motion-title">Do not show the bullet. Perform the idea.</h2>
    </section>
  );
}

function StepSix() {
  const soundCues = ["hit", "tick", "whoosh", "pop", "lock"];
  return (
    <section className="cal-scene cal-step-six">
      <FrameLabel step={5} label="sound locks frame" />
      <div className="cal-audio-board">
        <div className="cal-wave" aria-hidden>
          {Array.from({ length: 36 }).map((_, index) => (
            <i key={index} style={{ "--i": index } as React.CSSProperties} />
          ))}
        </div>
        <div className="cal-cues">
          {soundCues.map((cue, index) => (
            <div className="cal-cue" key={cue} style={{ "--i": index } as React.CSSProperties}>
              <span />
              <strong>{cue}</strong>
            </div>
          ))}
        </div>
      </div>
      <h2>Clicks start feeling like edit points.</h2>
    </section>
  );
}

function StepSeven() {
  return (
    <section className="cal-scene cal-step-seven">
      <FrameLabel step={6} label="one-take recording" />
      <div className="cal-record-frame">
        <div className="cal-rec-dot" />
        <span className="label-mono">REC · ?auto=1</span>
        <div className="cal-rec-stage">
          <strong>1920 x 1080</strong>
          <span>hidden chrome · one take</span>
        </div>
      </div>
      <div className="cal-record-rail">
        {Array.from({ length: 8 }).map((_, index) => (
          <i key={index} style={{ "--i": index } as React.CSSProperties} />
        ))}
      </div>
    </section>
  );
}

function StepEight() {
  const chain = ["article.md", "script.md", "motion-treatment.md", "presentation", "video.mp4"];
  return (
    <section className="cal-scene cal-step-eight">
      <FrameLabel step={7} label="final lockup" />
      <div className="cal-final-chain">
        {chain.map((item, index) => (
          <div className="cal-chain-item" key={item} style={{ "--i": index } as React.CSSProperties}>
            <span className="label-mono">{index === chain.length - 1 ? "output" : "stage"}</span>
            <strong>{item}</strong>
          </div>
        ))}
      </div>
      <div className="cal-final-copy">
        <p className="cal-kicker">Article In. Video Out.</p>
        <h2>Not a slide deck. A recordable video stage.</h2>
        <p>这不是 PPT。这是一个可录屏的视频舞台。</p>
      </div>
    </section>
  );
}

export default function CinematicAiLaunch({ step }: ChapterStepProps) {
  const safeStep = Math.max(0, Math.min(7, step));
  const scenes = useMemo(
    () => [
      <StepOne key="one" />,
      <StepTwo key="two" />,
      <StepThree key="three" />,
      <StepFour key="four" />,
      <StepFive key="five" />,
      <StepSix key="six" />,
      <StepSeven key="seven" />,
      <StepEight key="eight" />,
    ],
    [],
  );

  useEffect(() => {
    playCue(cues[safeStep]);
  }, [safeStep]);

  return <div className={`cal-root cal-step-${safeStep}`}>{scenes[safeStep]}</div>;
}
```

- [ ] **Step 2: Run typecheck and fix import/type errors**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
npx tsc --noEmit
```

Expected: no TypeScript errors.

- [ ] **Step 3: Commit the component**

Run:

```bash
git add showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.tsx
git commit -m "Implement cinematic AI launch chapter structure"
```

Expected: commit succeeds.

---

### Task 6: Implement Chapter Motion Styling

**Files:**
- Modify: `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.css`

- [ ] **Step 1: Replace CSS with full motion styling**

Replace `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.css`:

```css
.cal-root,
.cal-scene {
  width: 100%;
  height: 100%;
  color: var(--text);
}

.cal-root {
  position: relative;
  overflow: hidden;
}

.cal-root::before {
  content: "";
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 50% 45%, var(--accent-soft), transparent 32%),
    linear-gradient(110deg, transparent 0 52%, rgba(255,255,255,0.035) 52% 52.4%, transparent 52.4%);
  opacity: 0.8;
  pointer-events: none;
}

.cal-scene {
  position: relative;
  padding: var(--stage-pad-y) var(--stage-pad-x);
  box-sizing: border-box;
}

.cal-frame-label {
  position: absolute;
  top: 72px;
  left: var(--stage-pad-x);
  right: var(--stage-pad-x);
  display: flex;
  justify-content: space-between;
  color: var(--text-mute);
  text-transform: uppercase;
  letter-spacing: 0.14em;
  z-index: 10;
}

.cal-kicker {
  margin: 0;
  color: var(--accent);
  font-family: var(--font-mono);
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

.cal-step-one {
  display: grid;
  place-items: center;
}

.cal-impact-ring {
  position: absolute;
  width: 520px;
  height: 520px;
  border: 1px solid var(--accent);
  border-radius: 50%;
  opacity: 0;
  animation: cal-ring 1200ms ease-out forwards;
}

.cal-article-file {
  position: absolute;
  width: 270px;
  height: 340px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -52%) rotate(-7deg);
  background: var(--surface-2);
  border: 1px solid var(--rule);
  box-shadow: 0 30px 90px rgba(0, 0, 0, 0.35);
  animation: cal-file-drop 900ms cubic-bezier(.2, .9, .2, 1) forwards;
}

.cal-file-top {
  padding: 22px 26px;
  color: var(--accent);
  border-bottom: 1px solid var(--rule);
}

.cal-file-lines {
  padding: 28px 26px;
}

.cal-file-lines i {
  display: block;
  height: 10px;
  margin-bottom: 18px;
  background: var(--text-faint);
}

.cal-file-lines i:nth-child(1) { width: 82%; }
.cal-file-lines i:nth-child(2) { width: 58%; }
.cal-file-lines i:nth-child(3) { width: 74%; }
.cal-file-lines i:nth-child(4) { width: 48%; }
.cal-file-lines i:nth-child(5) { width: 66%; }

.cal-fragments span {
  --angle: calc((var(--i) * 43deg) - 140deg);
  position: absolute;
  left: 50%;
  top: 50%;
  font-family: var(--font-mono);
  color: var(--text-mute);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  transform: translate(-50%, -50%) rotate(var(--angle));
  animation: cal-fragment 1200ms cubic-bezier(.2, .8, .2, 1) forwards;
  animation-delay: calc(var(--i) * 48ms + 260ms);
  opacity: 0;
}

.cal-hero-lockup {
  position: absolute;
  left: var(--stage-pad-x);
  bottom: 120px;
}

.cal-hero-lockup h1 {
  margin: 18px 0 18px;
  display: flex;
  flex-direction: column;
  font-family: var(--font-display-en);
  font-size: 132px;
  line-height: .92;
  letter-spacing: 0;
}

.cal-hero-lockup p:last-child {
  margin: 0;
  color: var(--text-2);
  font-size: 34px;
}

.cal-step-two {
  display: grid;
  grid-template-columns: 1fr 520px;
  align-items: center;
  gap: 80px;
}

.cal-tangle {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.cal-tangle path {
  fill: none;
  stroke: var(--accent);
  stroke-width: 2;
  stroke-dasharray: 10 16;
  opacity: 0.45;
  stroke-dashoffset: 520;
  animation: cal-draw 1400ms ease-out forwards;
}

.cal-islands {
  position: relative;
  height: 650px;
}

.cal-island {
  position: absolute;
  width: 230px;
  padding: 26px;
  animation: cal-island 900ms cubic-bezier(.2, .8, .2, 1) forwards;
  animation-delay: calc(var(--i) * 90ms);
  opacity: 0;
}

.cal-island:nth-child(1) { left: 70px; top: 70px; }
.cal-island:nth-child(2) { left: 420px; top: 30px; }
.cal-island:nth-child(3) { left: 250px; top: 270px; }
.cal-island:nth-child(4) { left: 640px; top: 310px; }
.cal-island:nth-child(5) { left: 120px; top: 520px; }

.cal-island span,
.cal-island strong {
  display: block;
}

.cal-island strong {
  margin-top: 16px;
  font-family: var(--font-display-en);
  font-size: 42px;
}

.cal-side-copy h2,
.cal-statement,
.cal-motion-title,
.cal-step-six h2,
.cal-final-copy h2 {
  margin: 18px 0 0;
  font-family: var(--font-display-en);
  font-size: 82px;
  line-height: 1;
  max-width: 900px;
}

.cal-step-three {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.cal-pipeline {
  position: relative;
  height: 360px;
}

.cal-pipeline-source {
  position: absolute;
  left: 0;
  top: 112px;
  width: 250px;
  padding: 28px;
  z-index: 2;
}

.cal-pipeline-source strong {
  display: block;
  margin-top: 14px;
  font-size: 38px;
}

.cal-pipe-line {
  position: absolute;
  left: 270px;
  top: 90px;
  width: 1160px;
  height: 180px;
}

.cal-pipe-line path {
  fill: none;
  stroke: var(--accent);
  stroke-width: 4;
  stroke-dasharray: 1120;
  stroke-dashoffset: 1120;
  animation: cal-draw 1600ms ease-out forwards;
}

.cal-pipeline-nodes {
  position: absolute;
  left: 330px;
  right: 40px;
  top: 100px;
  display: flex;
  justify-content: space-between;
}

.cal-node {
  display: grid;
  justify-items: center;
  gap: 22px;
  opacity: 0;
  animation: cal-node 700ms ease-out forwards;
  animation-delay: calc(var(--i) * 160ms + 300ms);
}

.cal-node span {
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: var(--accent);
  box-shadow: 0 0 44px var(--accent-glow);
}

.cal-node strong {
  font-family: var(--font-mono);
  color: var(--text-2);
}

.cal-step-four {
  display: grid;
  grid-template-columns: 620px 1fr;
  gap: 80px;
  align-items: center;
}

.cal-script-panel {
  padding: 38px;
}

.cal-script-panel p {
  margin: 28px 0 0;
  color: var(--text-2);
  font-size: 30px;
  line-height: 1.45;
}

.cal-beat-stack {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 22px;
}

.cal-beat {
  padding: 24px;
  border: 1px solid var(--rule);
  background: var(--surface-2);
  transform-origin: left center;
  animation: cal-beat 650ms cubic-bezier(.2, .8, .2, 1) forwards;
  animation-delay: calc(var(--i) * 85ms);
  opacity: 0;
}

.cal-beat strong {
  display: block;
  margin-top: 12px;
  font-size: 32px;
}

.cal-step-five {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 64px;
}

.cal-motion-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 20px;
}

.cal-motion-card {
  min-height: 360px;
  padding: 24px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  animation: cal-card-rise 760ms ease-out forwards;
  animation-delay: calc(var(--i) * 90ms);
  opacity: 0;
}

.cal-motion-card strong {
  font-size: 34px;
  line-height: 1.05;
}

.cal-mini {
  height: 110px;
  position: relative;
  border-top: 1px solid var(--rule);
}

.cal-mini i {
  position: absolute;
  display: block;
  background: var(--accent);
}

.cal-mini-0 i {
  width: 44px;
  height: 8px;
  left: calc(20px + var(--i) * 54px);
  top: calc(30px + var(--i) * 20px);
  animation: cal-pulse 900ms ease-in-out infinite alternate;
}

.cal-mini-1 i {
  width: 72px;
  height: 2px;
  left: calc(20px + var(--i) * 44px);
  top: calc(72px - var(--i) * 18px);
  transform: rotate(calc(var(--i) * 18deg));
}

.cal-mini-2 i {
  width: 34px;
  height: 86px;
  left: calc(28px + var(--i) * 50px);
  bottom: 0;
  opacity: calc(.35 + var(--i) * .2);
}

.cal-mini-3 i {
  height: 10px;
  left: 20px;
  top: calc(28px + var(--i) * 24px);
  width: calc(60px + var(--i) * 34px);
}

.cal-mini-4 i {
  width: 58px;
  height: 28px;
  left: calc(18px + var(--i) * 58px);
  top: calc(30px + var(--i) * 16px);
  border-radius: 4px;
}

.cal-step-six {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 70px;
}

.cal-audio-board {
  padding: 54px;
  border: 1px solid var(--rule);
  background: var(--surface-2);
}

.cal-wave {
  height: 220px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.cal-wave i {
  width: 8px;
  height: calc(24px + (var(--i) % 9) * 18px);
  background: var(--text-faint);
  animation: cal-wave 900ms ease-in-out infinite alternate;
  animation-delay: calc(var(--i) * 28ms);
}

.cal-cues {
  display: flex;
  justify-content: space-between;
}

.cal-cue {
  display: grid;
  justify-items: center;
  gap: 14px;
  opacity: 0;
  animation: cal-node 600ms ease-out forwards;
  animation-delay: calc(var(--i) * 130ms + 280ms);
}

.cal-cue span {
  width: 18px;
  height: 18px;
  background: var(--accent);
  border-radius: 50%;
  box-shadow: 0 0 32px var(--accent-glow);
}

.cal-cue strong {
  font-family: var(--font-mono);
  color: var(--text-2);
}

.cal-step-seven {
  display: grid;
  place-items: center;
}

.cal-record-frame {
  position: relative;
  width: 1260px;
  height: 700px;
  border: 3px solid var(--accent);
  box-shadow: 0 0 70px var(--accent-glow), inset 0 0 90px rgba(0,0,0,.28);
  animation: cal-record 900ms ease-out forwards;
}

.cal-record-frame > .label-mono {
  position: absolute;
  top: 26px;
  left: 68px;
  color: var(--accent);
}

.cal-rec-dot {
  position: absolute;
  width: 18px;
  height: 18px;
  top: 31px;
  left: 34px;
  border-radius: 50%;
  background: var(--accent);
  animation: cal-blink 700ms infinite;
}

.cal-rec-stage {
  position: absolute;
  inset: 110px 90px 110px;
  display: grid;
  place-items: center;
  border: 1px solid var(--rule);
}

.cal-rec-stage strong {
  font-size: 92px;
  font-family: var(--font-display-en);
}

.cal-rec-stage span {
  color: var(--text-2);
  font-size: 28px;
}

.cal-record-rail {
  position: absolute;
  left: 300px;
  right: 300px;
  bottom: 112px;
  display: flex;
  justify-content: space-between;
}

.cal-record-rail i {
  width: 74px;
  height: 8px;
  background: var(--accent);
  opacity: 0;
  animation: cal-rail 900ms ease-out forwards;
  animation-delay: calc(var(--i) * 90ms);
}

.cal-step-eight {
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 86px;
}

.cal-final-chain {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 18px;
}

.cal-chain-item {
  min-height: 160px;
  padding: 28px;
  border: 1px solid var(--rule);
  background: var(--surface-2);
  animation: cal-card-rise 700ms ease-out forwards;
  animation-delay: calc(var(--i) * 100ms);
  opacity: 0;
}

.cal-chain-item strong {
  display: block;
  margin-top: 24px;
  font-size: 31px;
  line-height: 1.05;
}

.cal-chain-item:last-child {
  border-color: var(--accent);
  box-shadow: 0 0 42px var(--accent-glow);
}

.cal-final-copy p:last-child {
  margin: 24px 0 0;
  color: var(--text-2);
  font-size: 34px;
}

@keyframes cal-ring {
  0% { transform: scale(.4); opacity: 0; }
  25% { opacity: .8; }
  100% { transform: scale(1.9); opacity: 0; }
}

@keyframes cal-file-drop {
  0% { transform: translate(-50%, -180%) rotate(-14deg); opacity: 0; }
  80% { transform: translate(-50%, -50%) rotate(-7deg); opacity: 1; }
  100% { transform: translate(-50%, -52%) rotate(-7deg); opacity: 1; }
}

@keyframes cal-fragment {
  0% { opacity: 0; transform: translate(-50%, -50%) scale(.7) rotate(var(--angle)); }
  100% {
    opacity: .78;
    transform: translate(
      calc(-50% + cos(var(--angle)) * 560px),
      calc(-50% + sin(var(--angle)) * 300px)
    ) rotate(var(--angle));
  }
}

@keyframes cal-draw {
  to { stroke-dashoffset: 0; }
}

@keyframes cal-island {
  from { opacity: 0; transform: translateY(40px) scale(.94); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

@keyframes cal-node {
  from { opacity: 0; transform: translateY(24px) scale(.82); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

@keyframes cal-beat {
  from { opacity: 0; transform: translateX(-44px) scaleX(.8); }
  to { opacity: 1; transform: translateX(0) scaleX(1); }
}

@keyframes cal-card-rise {
  from { opacity: 0; transform: translateY(42px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes cal-pulse {
  from { opacity: .35; transform: translateX(0); }
  to { opacity: 1; transform: translateX(18px); }
}

@keyframes cal-wave {
  from { transform: scaleY(.45); opacity: .45; }
  to { transform: scaleY(1); opacity: .9; }
}

@keyframes cal-record {
  from { transform: scale(.92); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

@keyframes cal-blink {
  0%, 100% { opacity: 1; }
  50% { opacity: .28; }
}

@keyframes cal-rail {
  from { opacity: 0; transform: scaleX(.2); }
  to { opacity: .85; transform: scaleX(1); }
}
```

- [ ] **Step 2: Check CSS support for trigonometric functions**

Run the dev server and inspect step 1. If text fragments do not spread because the browser does not support CSS `sin()` / `cos()`, replace the `.cal-fragments span` positioning with explicit nth-child offsets:

```css
.cal-fragments span:nth-child(1) { transform: translate(-720px, -260px) rotate(-19deg); }
.cal-fragments span:nth-child(2) { transform: translate(-410px, -340px) rotate(8deg); }
.cal-fragments span:nth-child(3) { transform: translate(260px, -310px) rotate(-7deg); }
.cal-fragments span:nth-child(4) { transform: translate(620px, -190px) rotate(16deg); }
.cal-fragments span:nth-child(5) { transform: translate(-650px, 190px) rotate(11deg); }
.cal-fragments span:nth-child(6) { transform: translate(-280px, 320px) rotate(-13deg); }
.cal-fragments span:nth-child(7) { transform: translate(310px, 290px) rotate(6deg); }
.cal-fragments span:nth-child(8) { transform: translate(690px, 170px) rotate(-11deg); }
```

Expected: fragments visibly scatter from the article file.

- [ ] **Step 3: Run typecheck**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
npx tsc --noEmit
```

Expected: no TypeScript errors.

- [ ] **Step 4: Commit motion styling**

Run:

```bash
git add showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.css
git commit -m "Add cinematic motion styling"
```

Expected: commit succeeds.

---

### Task 7: Browser Review And Visual Polish

**Files:**
- Modify as needed: `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.tsx`
- Modify as needed: `showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch/CinematicAiLaunch.css`

- [ ] **Step 1: Start the dev server**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
npm run dev
```

Expected: Vite prints a local URL, usually `http://localhost:5173/` or `http://localhost:5174/`.

- [ ] **Step 2: Review all eight manual steps**

Open the local URL and advance through all steps using click or arrow keys.

Expected manual observations:

- Step 1: article file drops; fragments spread; lockup is legible.
- Step 2: five workflow islands appear; tangle lines draw.
- Step 3: pipeline line draws and nodes ignite.
- Step 4: script panel and beat blocks are both visible without crowding.
- Step 5: five motion cards animate and do not read as a plain feature grid.
- Step 6: waveform moves and cue markers land clearly.
- Step 7: recording frame feels like a captured 16:9 stage.
- Step 8: artifact chain reads clearly and final message lands.

- [ ] **Step 3: Fix any obvious visual issues**

If text is too small, increase the relevant font sizes in `CinematicAiLaunch.css`:

```css
.cal-chain-item strong {
  font-size: 34px;
}

.cal-script-panel p {
  font-size: 32px;
}
```

If any card feels too static, add `animation-delay` and a small transform to the affected selector:

```css
.cal-motion-card:hover {
  transform: translateY(-6px);
}
```

If sound cues are too loud, reduce volume in `playCue`:

```ts
audio.volume = id === "low-hit" || id === "lock" ? 0.22 : 0.16;
```

- [ ] **Step 4: Review auto/audio modes**

Run with audio mode:

```text
http://localhost:<port>/?audio=1
```

Run with auto mode:

```text
http://localhost:<port>/?auto=1
```

Expected:

- Manual navigation remains possible.
- In `audio=1`, narration audio may be missing, but local SFX still plays on step changes.
- In `auto=1`, the existing fallback duration advances steps even without narration mp3 files after the start gate.

- [ ] **Step 5: Commit polish changes**

Run:

```bash
git add showcases/cinematic-ai-launch/presentation/src/chapters/01-cinematic-ai-launch
git commit -m "Polish cinematic showcase playback"
```

Expected: commit succeeds if any polish changes were made. If no changes were needed, skip this commit.

---

### Task 8: Final Verification And Handoff Notes

**Files:**
- Create: `showcases/cinematic-ai-launch/README.md`

- [ ] **Step 1: Run final typecheck**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
npx tsc --noEmit
```

Expected: no TypeScript errors.

- [ ] **Step 2: Run narration extraction**

Run:

```bash
cd showcases/cinematic-ai-launch/presentation
npm run extract-narrations
```

Expected: `audio-segments.json` is generated and includes eight entries for `cinematic-ai-launch`.

- [ ] **Step 3: Write showcase README**

Create `showcases/cinematic-ai-launch/README.md`:

```markdown
# Cinematic AI Launch Showcase

This showcase proves the target quality bar for `web-video-presentation`: a recordable web stage that feels closer to a product launch video than a slide deck.

## Run

```bash
cd presentation
npm install
npm run dev
```

Manual mode:

```text
http://localhost:5173/
```

Audio preview mode:

```text
http://localhost:5173/?audio=1
```

Auto recording mode:

```text
http://localhost:5173/?auto=1
```

## Structure

- `article.md` is the source article.
- `script.md` is the spoken launch script.
- `outline.md` is the eight-step screen plan.
- `motion-treatment.md` defines the action design for each step.
- `sound-plan.md` defines local step sound cues.
- `presentation/` contains the Vite + React implementation.

## What This Demonstrates

- Each step has a dominant action.
- Transitions preserve visual continuity.
- Sound cues make step changes feel like edit points.
- The base template can support a commercial-looking video sample without becoming a full video engine.

## Follow-Up

After this showcase is accepted, fold the lessons back into the skill:

- Add `references/MOTION-TREATMENT.md`.
- Add `references/SOUND-DESIGN.md`.
- Update `references/CHAPTER-CRAFT.md` with a stronger anti-PPT checklist.
```

- [ ] **Step 4: Check git status**

Run:

```bash
git status --short
```

Expected: only expected files are modified or untracked.

- [ ] **Step 5: Commit README and generated audio segments**

Run:

```bash
git add showcases/cinematic-ai-launch/README.md \
  showcases/cinematic-ai-launch/presentation/audio-segments.json
git commit -m "Document cinematic AI launch showcase"
```

Expected: commit succeeds.

- [ ] **Step 6: Final manual review**

Open the dev server and perform one complete pass from step 1 to step 8.

Acceptance checklist:

- The first screen feels like a product launch cold open.
- No step is pure text.
- No step is only a title plus cards.
- Sound cues trigger on step changes without blocking navigation.
- `narrations.length` is eight and the component clamps step to `0..7`.
- The final lockup clearly sells article-to-video production.

---

## Self-Review

Spec coverage:

- Independent showcase directory: covered in Tasks 1-2.
- Commercial article-to-video product launch framing: covered in Task 1 content and Task 8 README.
- Eight-step cinematic chapter: covered in Tasks 3, 5, and 6.
- Lightweight local sound cues: covered in Task 4 and `playCue` in Task 5.
- No base template audio redesign: preserved by keeping sound logic inside the showcase chapter.
- Verification: covered in Tasks 7-8.
- Follow-up skill upgrades: documented in Task 8 README and the design spec remains the source for later documentation updates.

Placeholder scan:

- No task contains unresolved placeholder language.
- Every file creation task includes concrete content.
- Every verification step includes exact commands and expected results.

Type consistency:

- Chapter id is consistently `cinematic-ai-launch`.
- Component is consistently `CinematicAiLaunch`.
- CSS prefix is consistently `.cal-`.
- SFX cue ids match generated WAV file names and `sound-plan.md`.
